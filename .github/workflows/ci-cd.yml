name: MLOps CI/CD Pipeline

# Trigger on every push to main
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Set up Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: pip install -r requirements.txt

      # Step 4: Run tests (optional)
      - name: Run tests
        run: pytest || echo "No tests yet"

      # Step 5: Build Docker image
      - name: Build Docker image
        run: |
          docker build -t churn-api .

      # Step 6: Optional: Run container locally (for testing in CI)
      - name: Run Docker container
        run: docker run -d -p 8003:8003 churn-api

      # Step 7: (Optional) Docker Hub push - add secrets for USERNAME + PASSWORD
      # - name: Login DockerHub
      #   run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
      # - name: Tag & Push
      #   run: |
      #     docker tag churn-api ${{ secrets.DOCKER_USERNAME }}/churn-api:latest
      #     docker push ${{ secrets.DOCKER_USERNAME }}/churn-api:latest
